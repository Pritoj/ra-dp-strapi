{
  "name": "ra-dp-strapi",
  "version": "0.1.0",
  "description": "A Strapi Data Provider for react-admin",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:prod": "NODE_ENV=production BABEL_ENV=$NODE_ENV babel src --presets babel-preset-es2015 --out-dir dist",
    "prepublish": "npm run build:prod"
  },
  "keywords": [
    "strapi",
    "react-admin",
    "admin-on-react",
    "data-provider"
  ],
  "author": "Pritoj Singh <pritojs@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
